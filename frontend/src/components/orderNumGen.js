import React from 'react';
import ReactDOM from 'react-dom/client';
import { useEffect, useState } from 'react';

export const BasketInit = (custNum) => {
    const [orderNumber, setOrderNumber] = useState([])
    
}